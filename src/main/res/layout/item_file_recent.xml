<?xml version="1.0" encoding="utf-8"?>
<!-- item_file_recent.xml — Card de arquivo recente (lista horizontal) -->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="110dp"
    android:layout_height="120dp"
    android:layout_marginHorizontal="4dp"
    app:cardBackgroundColor="#1A1D27"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeColor="#2A2D3A"
    app:strokeWidth="1dp"
    app:rippleColor="#2A2D3A">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="12dp">

        <!-- Ícone grande -->
        <FrameLayout
            android:layout_width="52dp"
            android:layout_height="52dp">

            <com.google.android.material.card.MaterialCardView
                android:layout_width="52dp"
                android:layout_height="52dp"
                app:cardBackgroundColor="#0F1117"
                app:cardCornerRadius="14dp"
                app:cardElevation="0dp"
                app:strokeWidth="0dp">

                <ImageView
                    android:id="@+id/ivRecentIcon"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:layout_gravity="center"
                    android:src="@mipmap/ic_image"
                    android:contentDescription="Arquivo recente"
                    tools:src="@mipmap/ic_video" />

            </com.google.android.material.card.MaterialCardView>

        </FrameLayout>

        <!-- Nome do arquivo -->
        <TextView
            android:id="@+id/tvRecentFileName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="arquivo.png"
            android:textColor="#FFFFFF"
            android:textSize="11sp"
            android:fontFamily="sans-serif-medium"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center" />

        <!-- Tamanho -->
        <TextView
            android:id="@+id/tvRecentFileSize"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="1,2 MB"
            android:textColor="#4A4D60"
            android:textSize="10sp" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>